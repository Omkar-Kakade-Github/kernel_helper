# Legacy

`old_sample.cuh` is the original experimental implementation retained for
historical reference. It is unsupported, untested, and excluded from the
installed `kernel_helper` package.

Use the headers under `include/kernel_helper/` for all new code.

